# 基于 YOLO 的简易口罩检测系统项目设计文档

## 1. 项目概述

本项目计划设计并实现一个基于 YOLO 目标检测算法的简易口罩检测系统。系统以 Python 作为主要开发语言，通过图形化界面完成用户交互，支持用户上传本地图片并进行口罩佩戴状态检测。

项目当前阶段主要实现图片检测功能，不包含实时摄像头检测。检测任务采用二分类目标检测方式，即识别图片中人脸区域是否处于以下两种状态：

- `mask`：佩戴口罩
- `no_mask`：未佩戴口罩

系统最终应能够完成图片上传、模型推理、检测框绘制、检测类别显示、置信度显示以及结果图片展示等基础功能。

## 2. 项目目标

### 2.1 功能目标

项目需要实现以下核心功能：

1. 支持用户通过图形化界面选择本地图片。
2. 加载训练完成的 YOLO 口罩检测模型。
3. 对上传图片进行目标检测。
4. 在图片中标出检测框、类别名称和置信度。
5. 在图形化界面中展示检测结果。
6. 支持用户更换图片并重复检测。

### 2.2 技术目标

项目需要完成以下技术流程：

1. 搭建 Python 开发环境。
2. 准备并整理口罩检测数据集。
3. 将数据集转换为 YOLO 所需格式。
4. 使用 YOLO 模型完成训练。
5. 评估模型检测效果。
6. 将训练好的模型集成到 Python 项目中。
7. 使用 GUI 框架完成可视化系统界面。

### 2.3 项目边界

当前版本暂不实现以下功能：

1. 摄像头实时检测。
2. 视频文件检测。
3. 多种口罩佩戴姿态细分类。
4. 用户登录、权限管理、数据库存储。
5. 云端部署或 Web 服务接口。

这些功能可以作为后续扩展方向。

## 3. 技术选型

### 3.1 开发语言

项目主体语言选择 Python，原因如下：

- YOLO 相关生态成熟，便于模型训练和推理。
- OpenCV、Pillow、PyQt、Tkinter 等图像处理和 GUI 库支持完善。
- 代码编写效率较高，适合快速完成原型系统。

建议 Python 版本：

```text
Python 3.9 或 Python 3.10
```

### 3.2 目标检测算法

推荐使用 Ultralytics YOLO 系列，例如 YOLOv8 或 YOLOv11。对于课程设计、毕业设计或小型项目而言，YOLOv8 使用资料丰富、接口清晰、部署简单，比较适合作为首选。

推荐模型：

```text
yolov8n.pt
```

选择 `yolov8n` 的原因：

- 模型体积小。
- 推理速度快。
- 对硬件要求较低。
- 适合简易图片检测系统。

如果后续追求更高精度，可以尝试：

```text
yolov8s.pt
yolov8m.pt
```

### 3.3 图形化界面框架

可选方案包括：

| 框架 | 优点 | 缺点 | 推荐程度 |
| --- | --- | --- | --- |
| Tkinter | Python 标准库自带，简单易用 | 界面美观度一般 | 适合快速实现 |
| PyQt5 / PyQt6 | 功能强大，界面效果更好 | 学习成本略高 | 推荐 |
| CustomTkinter | 比 Tkinter 更现代 | 需要额外安装 | 可选 |

如果项目重点是算法流程和系统完整性，推荐使用 Tkinter；如果需要更美观的课程设计展示效果，推荐使用 PyQt5。

本文档推荐采用：

```text
PyQt5
```

### 3.4 图像处理库

推荐使用以下库：

```text
opencv-python
Pillow
numpy
```

其中：

- OpenCV 用于读取图片、绘制检测框、颜色空间转换。
- Pillow 用于 GUI 中图片显示格式转换。
- NumPy 用于图像数组处理。

### 3.5 模型训练框架

推荐使用：

```text
ultralytics
```

安装后可以通过命令行或 Python 代码调用 YOLO 模型完成训练和推理。

## 4. 环境搭建流程

### 4.1 创建项目目录

建议项目命名为：

```text
mask_detect
```

推荐初始目录结构如下：

```text
mask_detect/
├── datasets/
│   └── mask_dataset/
├── models/
├── runs/
├── scripts/
│   ├── check_gpu.py
│   └── train.py
├── src/
│   ├── main.py
│   ├── detector.py
│   ├── ui.py
│   └── utils.py
├── environment.yml
├── requirements.txt
├── README.md
└── 项目设计文档.md
```

目录说明：

| 路径 | 作用 |
| --- | --- |
| `datasets/` | 存放训练数据集 |
| `models/` | 存放训练完成的模型权重 |
| `runs/` | 存放 YOLO 训练输出结果 |
| `scripts/check_gpu.py` | 检查 PyTorch、Ultralytics 和 GPU 是否可用 |
| `scripts/train.py` | YOLO 训练脚本，适合本地验证和服务器训练 |
| `src/main.py` | 程序入口 |
| `src/detector.py` | 模型加载和图片检测逻辑 |
| `src/ui.py` | 图形化界面逻辑 |
| `src/utils.py` | 工具函数 |
| `environment.yml` | Anaconda / Miniconda 环境配置 |
| `requirements.txt` | pip 依赖清单，作为备用安装方式 |
| `README.md` | 项目说明 |

### 4.2 创建 Conda 虚拟环境

项目虚拟环境建议使用 Anaconda 或 Miniconda 管理。推荐环境名称：

```text
mask-detect
```

推荐 Python 版本：

```text
Python 3.10
```

在项目根目录下创建 `environment.yml`：

```yaml
name: mask-detect
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - pillow
  - pyqt
  - pip:
      - ultralytics>=8.0.0
      - opencv-python>=4.8.0
```

然后执行：

```bash
conda env create -f environment.yml
conda activate mask-detect
```

如果不使用 `environment.yml`，也可以手动创建环境：

```bash
conda create -n mask-detect python=3.10
conda activate mask-detect
```

### 4.3 安装依赖

如果通过 `environment.yml` 创建环境，依赖会在创建环境时自动安装。

如果选择手动创建 conda 环境，可以在激活环境后执行：

```bash
pip install ultralytics opencv-python pillow numpy pyqt5
```

也可以使用项目中的 `requirements.txt` 安装：

```bash
pip install -r requirements.txt
```

### 4.4 导出或更新环境文件

如果后续新增依赖，推荐优先更新 `environment.yml`。也可以导出当前 conda 环境作为参考：

```bash
conda env export --from-history > environment.yml
```

如果需要更新 pip 依赖清单，可以执行：

```bash
pip freeze > requirements.txt
```

### 4.5 验证 YOLO 环境

可以执行以下命令验证环境是否可用：

```bash
yolo predict model=yolov8n.pt source=test.jpg
```

如果能够正常完成预测并生成结果图片，说明 conda 环境和 YOLO 依赖基本可用。

## 5. 数据集准备

### 5.1 数据集需求

本项目需要用于训练口罩检测模型的图片数据。每张图片中应包含人脸目标，并标注其是否佩戴口罩。

类别设计如下：

```text
0 mask
1 no_mask
```

其中：

- `mask` 表示正确或基本正确佩戴口罩的人脸。
- `no_mask` 表示未佩戴口罩的人脸。

### 5.2 数据来源

数据集可以通过以下方式获取：

1. 使用公开口罩检测数据集。
2. 从 Kaggle、GitHub、Roboflow 等平台下载公开数据。
3. 自行收集图片并手动标注。
4. 混合使用公开数据和自建数据。

常见数据集关键词：

```text
face mask detection dataset
mask detection yolo dataset
with mask without mask dataset
```

### 5.3 数据质量要求

为了提高模型泛化能力，数据集应尽量包含以下情况：

1. 正面人脸、侧脸、低头、抬头等不同姿态。
2. 单人图片和多人图片。
3. 室内、室外、强光、弱光等不同光照条件。
4. 不同年龄、性别、肤色的人脸样本。
5. 普通医用口罩、N95 口罩、布口罩等不同口罩类型。
6. 遮挡、模糊、远距离等较复杂场景。

### 5.4 标注工具

推荐使用以下标注工具：

| 工具 | 说明 |
| --- | --- |
| LabelImg | 简单易用，支持 YOLO 格式 |
| Labelme | 功能较丰富，但需要格式转换 |
| Roboflow | 在线标注和数据集管理方便 |
| CVAT | 适合较大规模标注任务 |

对于本项目，推荐使用 LabelImg 或 Roboflow。

### 5.5 YOLO 数据格式

YOLO 每张图片需要对应一个同名 `.txt` 标注文件。例如：

```text
images/train/001.jpg
labels/train/001.txt
```

标注文件内容格式如下：

```text
class_id center_x center_y width height
```

其中坐标均为归一化后的比例值，范围为 0 到 1。

示例：

```text
0 0.512 0.438 0.214 0.286
1 0.725 0.501 0.180 0.240
```

含义：

- 第一行表示一个佩戴口罩的人脸目标。
- 第二行表示一个未佩戴口罩的人脸目标。

### 5.6 数据集目录结构

YOLO 推荐数据集结构如下：

```text
datasets/mask_dataset/
├── images/
│   ├── train/
│   ├── val/
│   └── test/
├── labels/
│   ├── train/
│   ├── val/
│   └── test/
└── mask.yaml
```

说明：

| 目录 | 作用 |
| --- | --- |
| `images/train/` | 训练图片 |
| `images/val/` | 验证图片 |
| `images/test/` | 测试图片 |
| `labels/train/` | 训练标注 |
| `labels/val/` | 验证标注 |
| `labels/test/` | 测试标注 |
| `mask.yaml` | YOLO 数据集配置文件 |

### 5.7 数据集划分

推荐比例：

```text
训练集：70%
验证集：20%
测试集：10%
```

如果数据量较小，也可以使用：

```text
训练集：80%
验证集：20%
```

测试集可以后续单独补充。

### 5.8 数据集配置文件

`mask.yaml` 示例：

```yaml
path: datasets/mask_dataset
train: images/train
val: images/val
test: images/test

names:
  0: mask
  1: no_mask
```

如果不使用测试集，可以去掉 `test` 字段：

```yaml
path: datasets/mask_dataset
train: images/train
val: images/val

names:
  0: mask
  1: no_mask
```

## 6. 模型训练流程

### 6.1 选择预训练模型

推荐使用 YOLOv8 预训练模型作为初始权重：

```text
yolov8n.pt
```

预训练模型已经在大规模数据集上学习了通用视觉特征，在口罩检测数据集上进行微调，可以减少训练时间并提升效果。

### 6.2 训练命令

正式训练建议在 GPU 服务器上执行。训练前先检查 GPU 和依赖：

```bash
python scripts/check_gpu.py
```

进入项目根目录后，执行：

```bash
python scripts/train.py --device 0 --epochs 100 --imgsz 640 --batch 16
```

参数说明：

| 参数 | 说明 |
| --- | --- |
| `model` | 使用的初始模型权重 |
| `data` | 数据集配置文件路径 |
| `epochs` | 训练轮数 |
| `imgsz` | 输入图片尺寸 |
| `batch` | 批量大小 |
| `device` | 训练设备，`0` 表示第 1 张 GPU，`cpu` 表示 CPU |

### 6.3 参数建议

初始训练建议：

```text
epochs: 50 - 100
imgsz: 640
batch: 8 或 16
model: yolov8n.pt
```

如果显存不足，可以降低：

```text
batch=4
imgsz=416
```

如果使用 CPU 训练，训练速度会明显降低，建议减少训练轮数或使用更小的数据集进行测试。

CPU 仅建议用于流程验证，例如：

```bash
python scripts/train.py --device cpu --epochs 5 --imgsz 416 --batch 2
```

正式训练建议使用 GPU，例如：

```bash
python scripts/train.py --device 0 --epochs 100 --imgsz 640 --batch 16
```

### 6.4 训练输出

训练完成后，YOLO 会在 `runs/detect/mask_train/` 目录下生成训练结果。常见文件包括：

```text
runs/detect/mask_train/
├── weights/
│   ├── best.pt
│   └── last.pt
├── results.png
├── confusion_matrix.png
├── F1_curve.png
├── P_curve.png
├── R_curve.png
└── PR_curve.png
```

其中：

| 文件 | 说明 |
| --- | --- |
| `best.pt` | 验证集效果最好的模型 |
| `last.pt` | 最后一轮训练得到的模型 |
| `results.png` | 训练过程指标变化 |
| `confusion_matrix.png` | 混淆矩阵 |
| `PR_curve.png` | 精确率-召回率曲线 |

项目集成时优先使用：

```text
runs/detect/mask_train/weights/best.pt
```

如果使用原生 YOLO 命令训练，输出目录可能是 `runs/detect/train/`，以实际控制台输出为准。

### 6.5 模型评估

训练完成后，可以使用验证命令评估模型：

```bash
yolo detect val model=runs/detect/mask_train/weights/best.pt data=datasets/mask_dataset/mask.yaml
```

重点关注以下指标：

| 指标 | 含义 |
| --- | --- |
| Precision | 检测为某类的目标中有多少是正确的 |
| Recall | 真实目标中有多少被成功检测出来 |
| mAP50 | IoU 阈值为 0.5 时的平均精度 |
| mAP50-95 | 多个 IoU 阈值下的平均精度 |

对于简易口罩检测系统，`mAP50` 是较直观的参考指标。

### 6.6 测试单张图片

可以使用以下命令测试模型：

```bash
yolo detect predict model=runs/detect/mask_train/weights/best.pt source=test.jpg conf=0.25
```

其中 `conf=0.25` 表示置信度阈值为 0.25。实际项目中可以根据效果调整为：

```text
0.3 - 0.5
```

### 6.7 远程 GPU 服务器训练流程

如果本地电脑没有独立显卡，推荐采用“本地开发、服务器训练、本地集成”的方式。

本地主要负责：

1. 编写和调试 GUI 程序。
2. 整理数据集目录。
3. 检查 `mask.yaml` 配置。
4. 使用少量样本进行 CPU 流程验证。

服务器主要负责：

1. 安装 GPU 版本深度学习环境。
2. 运行 YOLO 正式训练。
3. 输出 `best.pt` 模型权重。

推荐操作流程：

1. 在本地整理好项目和数据集。
2. 将项目目录上传到服务器。
3. 在服务器创建 conda 环境：

```bash
conda env create -f environment.yml
conda activate mask-detect
```

4. 检查 GPU：

```bash
nvidia-smi
python scripts/check_gpu.py
```

5. 在服务器启动训练：

```bash
python scripts/train.py --device 0 --epochs 100 --imgsz 640 --batch 16
```

6. 训练完成后，下载最佳模型：

```text
runs/detect/mask_train/weights/best.pt
```

7. 将模型放回本地项目：

```text
models/mask_yolo.pt
```

这样本地 GUI 程序就可以直接加载服务器训练好的模型进行图片检测。

远程训练注意事项：

1. `datasets/mask_dataset/mask.yaml` 中尽量使用相对路径。
2. 本地和服务器上的项目目录结构保持一致。
3. 不建议将完整数据集、`runs/` 训练结果和 `.pt` 权重提交到 Git。
4. 如果服务器 CUDA 环境特殊，需要按服务器要求单独安装匹配版本的 PyTorch。
5. 如果显存不足，优先降低 `batch`，其次降低 `imgsz`。

## 7. 模型集成设计

### 7.1 模型加载模块

建议在 `src/detector.py` 中封装模型加载和检测逻辑。

主要职责：

1. 加载 YOLO 权重文件。
2. 接收图片路径。
3. 执行模型推理。
4. 解析检测结果。
5. 返回绘制后的图片和检测信息。

核心流程如下：

```text
初始化检测器
    ↓
加载 best.pt 模型
    ↓
接收用户上传图片路径
    ↓
执行 YOLO 推理
    ↓
获取检测框、类别、置信度
    ↓
绘制结果图片
    ↓
返回 GUI 展示
```

### 7.2 检测结果数据结构

建议将每个检测目标整理为字典：

```python
{
    "class_id": 0,
    "class_name": "mask",
    "confidence": 0.92,
    "bbox": [x1, y1, x2, y2]
}
```

如果图片中包含多个目标，则返回列表：

```python
[
    {
        "class_id": 0,
        "class_name": "mask",
        "confidence": 0.92,
        "bbox": [100, 80, 220, 240]
    },
    {
        "class_id": 1,
        "class_name": "no_mask",
        "confidence": 0.88,
        "bbox": [300, 90, 410, 250]
    }
]
```

### 7.3 检测框绘制规则

建议使用不同颜色区分类别：

| 类别 | 颜色 |
| --- | --- |
| `mask` | 绿色 |
| `no_mask` | 红色 |

显示文本格式：

```text
mask 0.92
no_mask 0.88
```

这样用户可以直观看到每个目标的类别和置信度。

### 7.4 异常处理

模型集成时需要考虑以下异常情况：

1. 模型文件不存在。
2. 用户未选择图片就点击检测。
3. 图片格式不支持。
4. 图片读取失败。
5. 检测结果为空。
6. 模型推理过程发生错误。

建议在 GUI 中通过弹窗或状态栏提示用户。

## 8. 图形化界面设计

### 8.1 界面功能模块

图形化界面建议包含以下区域：

1. 图片显示区域。
2. 操作按钮区域。
3. 检测结果文本区域。
4. 状态提示区域。

### 8.2 界面按钮设计

建议设置以下按钮：

| 按钮 | 功能 |
| --- | --- |
| 选择图片 | 打开文件选择框，选择待检测图片 |
| 开始检测 | 调用模型进行检测 |
| 清空结果 | 清除当前图片和检测结果 |
| 退出系统 | 关闭程序 |

### 8.3 界面布局示例

推荐布局：

```text
┌──────────────────────────────────────────────┐
│              口罩检测系统                     │
├──────────────────────────────────────────────┤
│                                              │
│              图片显示区域                     │
│                                              │
├──────────────────────────────────────────────┤
│ [选择图片] [开始检测] [清空结果] [退出系统]    │
├──────────────────────────────────────────────┤
│ 检测结果：                                   │
│ 1. mask，置信度：0.92                         │
│ 2. no_mask，置信度：0.88                      │
└──────────────────────────────────────────────┘
```

### 8.4 图片显示逻辑

用户上传图片后，界面应先显示原始图片。点击开始检测后，界面更新为带检测框的结果图片。

流程如下：

```text
用户点击选择图片
    ↓
弹出文件选择窗口
    ↓
读取图片路径
    ↓
在界面显示原图
    ↓
用户点击开始检测
    ↓
调用检测模块
    ↓
显示检测结果图
    ↓
更新检测结果文本
```

### 8.5 检测结果展示

结果区域建议展示：

1. 检测目标数量。
2. 每个目标的类别。
3. 每个目标的置信度。
4. 可选显示检测框坐标。

示例：

```text
检测完成，共检测到 2 个目标。
1. 类别：mask，置信度：0.92
2. 类别：no_mask，置信度：0.88
```

如果没有检测到目标：

```text
未检测到口罩相关目标，请更换图片或降低置信度阈值。
```

## 9. 项目模块设计

### 9.1 主程序模块

文件：

```text
src/main.py
```

职责：

1. 初始化应用程序。
2. 创建 GUI 主窗口。
3. 启动事件循环。

### 9.2 检测模块

文件：

```text
src/detector.py
```

职责：

1. 加载 YOLO 模型。
2. 对图片进行推理。
3. 解析检测结果。
4. 绘制检测框。
5. 返回检测后的图片和结构化结果。

### 9.3 界面模块

文件：

```text
src/ui.py
```

职责：

1. 构建主窗口界面。
2. 响应按钮点击事件。
3. 调用检测模块。
4. 更新图片显示区域。
5. 更新检测结果文本。

### 9.4 工具模块

文件：

```text
src/utils.py
```

职责：

1. 图片格式转换。
2. 图片缩放。
3. 文件路径检查。
4. 检测结果格式化。

## 10. 系统运行流程

完整系统运行流程如下：

```text
启动程序
    ↓
初始化 GUI
    ↓
加载 YOLO 模型
    ↓
用户选择图片
    ↓
显示原始图片
    ↓
用户点击开始检测
    ↓
执行模型推理
    ↓
绘制检测框和类别文本
    ↓
显示结果图片
    ↓
展示检测统计信息
```

如果模型加载失败：

```text
启动程序
    ↓
检查模型文件
    ↓
模型不存在
    ↓
提示用户模型文件缺失
    ↓
终止检测功能或退出程序
```

如果用户未选择图片：

```text
点击开始检测
    ↓
检查当前图片路径
    ↓
图片路径为空
    ↓
提示用户先选择图片
```

## 11. 训练与应用阶段关系

项目可以分为两个阶段：

### 11.1 模型训练阶段

该阶段主要面向开发者，完成数据集准备和模型训练。

流程：

```text
收集图片
    ↓
标注数据
    ↓
划分数据集
    ↓
编写 mask.yaml
    ↓
执行 YOLO 训练
    ↓
评估模型效果
    ↓
保存 best.pt
```

### 11.2 系统应用阶段

该阶段面向最终用户，只需要使用训练好的模型进行检测。

流程：

```text
启动图形化系统
    ↓
选择图片
    ↓
点击检测
    ↓
查看检测结果
```

最终系统不需要用户了解模型训练细节，只需要保证模型文件存在即可。

## 12. 推荐开发步骤

### 12.1 第一阶段：环境和数据准备

1. 使用 Anaconda / Miniconda 创建 `mask-detect` 虚拟环境。
2. 通过 `environment.yml` 或 `requirements.txt` 安装 YOLO、OpenCV、PyQt5 等依赖。
3. 下载或准备口罩检测数据集。
4. 使用标注工具检查标注质量。
5. 整理为 YOLO 数据集格式。
6. 编写 `mask.yaml` 配置文件。

### 12.2 第二阶段：模型训练

1. 使用 `yolov8n.pt` 进行初始训练。
2. 观察训练过程中的 loss 和 mAP。
3. 使用验证集评估模型。
4. 使用测试图片进行人工检查。
5. 将最佳模型 `best.pt` 复制到 `models/` 目录。

建议模型路径：

```text
models/mask_yolo.pt
```

### 12.3 第三阶段：检测模块开发

1. 编写模型加载代码。
2. 编写图片检测函数。
3. 编写检测结果解析逻辑。
4. 编写检测框绘制逻辑。
5. 使用单张图片测试检测模块。

### 12.4 第四阶段：GUI 开发

1. 创建主窗口。
2. 添加图片显示区域。
3. 添加选择图片按钮。
4. 添加开始检测按钮。
5. 添加结果文本显示区域。
6. 将 GUI 与检测模块连接。
7. 测试完整流程。

### 12.5 第五阶段：优化和完善

1. 优化界面布局。
2. 增加异常提示。
3. 增加检测结果保存功能。
4. 编写项目 README。
5. 整理项目目录。
6. 测试不同图片场景。

## 13. 可能遇到的问题与解决方案

### 13.1 模型训练效果差

可能原因：

1. 数据量太少。
2. 标注不准确。
3. 类别分布不均衡。
4. 图片场景过于单一。
5. 训练轮数不足。

解决方案：

1. 增加训练数据。
2. 检查并修正标注文件。
3. 保证 `mask` 和 `no_mask` 样本数量相对均衡。
4. 增加不同光照、角度、距离的图片。
5. 适当增加 `epochs`。

### 13.2 模型误检较多

可能原因：

1. 置信度阈值过低。
2. 数据集中存在错误标注。
3. 背景干扰较强。

解决方案：

1. 提高 `conf` 阈值，例如从 0.25 提高到 0.4。
2. 清洗数据集。
3. 增加负样本或复杂场景样本。

### 13.3 小目标检测效果差

可能原因：

1. 图片中人脸太小。
2. 输入尺寸较低。
3. 训练集中小目标样本不足。

解决方案：

1. 将 `imgsz` 提高到 768 或 960。
2. 增加远距离人脸样本。
3. 使用更大的模型，例如 `yolov8s.pt`。

### 13.4 GUI 显示图片变形

可能原因：

1. 直接拉伸图片到固定控件大小。
2. 未保持原始宽高比。

解决方案：

1. 显示图片时按比例缩放。
2. 控件中居中显示图片。
3. 避免强制改变图片宽高比。

### 13.5 模型文件找不到

可能原因：

1. 模型路径写错。
2. `best.pt` 未复制到项目目录。
3. 程序运行路径与预期不一致。

解决方案：

1. 使用相对项目根目录的固定路径。
2. 启动时检查模型文件是否存在。
3. 在错误提示中显示当前查找路径。

## 14. 测试方案

### 14.1 功能测试

需要测试以下功能：

| 测试项 | 预期结果 |
| --- | --- |
| 启动程序 | GUI 正常显示 |
| 选择图片 | 图片正常加载并显示 |
| 点击检测 | 模型完成推理并展示结果 |
| 检测多人图片 | 能显示多个检测框 |
| 检测无目标图片 | 给出无目标提示 |
| 清空结果 | 图片和文本结果被清除 |
| 退出系统 | 程序正常关闭 |

### 14.2 模型测试

需要准备不同类型测试图片：

1. 单人佩戴口罩图片。
2. 单人未戴口罩图片。
3. 多人混合图片。
4. 侧脸图片。
5. 光线较暗图片。
6. 背景复杂图片。
7. 没有人脸的图片。

### 14.3 异常测试

需要测试以下异常情况：

| 场景 | 预期处理 |
| --- | --- |
| 未选择图片就检测 | 提示先选择图片 |
| 选择非图片文件 | 提示文件格式错误 |
| 模型文件不存在 | 提示模型缺失 |
| 图片读取失败 | 提示图片加载失败 |
| 检测结果为空 | 显示未检测到目标 |

## 15. 项目成果形式

项目最终可以包含以下成果：

```text
mask_detect/
├── datasets/
│   └── mask_dataset/
├── models/
│   └── mask_yolo.pt
├── runs/
├── scripts/
│   ├── check_gpu.py
│   └── train.py
├── src/
│   ├── main.py
│   ├── detector.py
│   ├── ui.py
│   └── utils.py
├── environment.yml
├── requirements.txt
├── README.md
└── 项目设计文档.md
```

最终运行方式：

```bash
python src/main.py
```

用户运行程序后，可以通过图形化界面上传图片并查看口罩检测结果。

## 16. 后续扩展方向

项目完成基础图片检测后，可以继续扩展以下功能：

1. 增加摄像头实时检测。
2. 增加视频文件检测。
3. 增加批量图片检测。
4. 增加检测结果保存功能。
5. 增加检测历史记录。
6. 增加模型选择功能。
7. 增加置信度阈值调节功能。
8. 将桌面程序打包为可执行文件。
9. 使用 Flask 或 FastAPI 改造为 Web 系统。
10. 增加更多类别，例如错误佩戴口罩。

## 17. 总结

本项目的核心思路是先通过 YOLO 完成口罩佩戴状态检测模型的训练，再将训练好的模型集成到 Python 图形化界面程序中。系统当前聚焦于上传图片检测，整体实现难度适中，技术路线清晰，适合用于课程设计、实验项目或小型应用原型。

推荐优先完成以下最小可用版本：

1. 准备 YOLO 格式数据集。
2. 训练得到 `best.pt` 模型。
3. 编写图片检测模块。
4. 编写 GUI 上传和显示功能。
5. 完成单张图片检测闭环。

在最小可用版本稳定后，再逐步增加结果保存、阈值调节、摄像头检测等扩展功能。
